"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
setup = "npm install"
run_mode = "concurrent"

[scripts.run.dev]
command = "npm run dev"
default = true
icon = "shield-check"

[scripts.run.test]
command = "npm test"
icon = "test-tube"
